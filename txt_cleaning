/* sometimes a string may have hidden char in it, like an 'enter' mark, HX='0D'x, which cause variables looked the same, but cannot match with each other. below are some methods to clean it */

patid = compress(patid,'03050A0C0D2009'x);
